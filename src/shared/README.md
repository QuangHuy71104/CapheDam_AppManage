# Shared layer

`shared/` contains code that is reusable across business features and has no dependency on a specific screen.

- `lib/`: pure utilities
- `ui/`: design tokens / shared UI primitives
- `api/`: authenticated HTTP clients
